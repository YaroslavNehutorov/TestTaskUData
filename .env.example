# Copy to .env and adjust as needed.
# For Docker: defaults (db, 5432, postgres/postgres/udata) work with docker-compose.

PROJECT_NAME=Udata Task

POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=udata
